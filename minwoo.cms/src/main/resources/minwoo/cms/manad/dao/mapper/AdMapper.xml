<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd"> 
<mapper namespace="minwoo.cms.manda.dao.mapper.AdMapper">
<select id="getAds" resultType="minwoo.cms.manad.domain.Ad">
	select ads_id adsId, ads_company adsCompany,ads_man adsMan,
	ads_money adsMoney, ads_start_date adsStartDate, ads_end_date adsEndDate
	from ads order by ads_id desc
</select>

<select id="getAd" parameterType="int" resultType="minwoo.cms.manad.domain.Ad">
	select ads_id adsId, ads_company adsCompany,ads_man adsMan,
	ads_money adsMoney, ads_start_date adsStartDate, ads_end_date adsEndDate
	from ads
	where ads_Id=#{adsId}

</select>
	<insert id="addAd" parameterType="string">
		insert into ads(ads_id,ads_company) values(adsid_seq.nextval,#{adsCompany})
	</insert>
	
	<update id="updateUser" parameterType="minwoo.cms.manad.domain.Ad">
		update ads
		set ads_company=#{adsCompany}
		where ads_id=#{adsId}
	
	</update>
	
	<delete id="delAd" parameterType="int">
			delete ads where ads_id=#{adsId}
	</delete>

</mapper>